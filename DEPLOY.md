# 🚀 মতিউর রহমান মল্লিক আর্কাইভ - cPanel Deployment Guide

এই গাইডে cPanel এ Next.js প্রজেক্ট deploy করার সহজ নির্দেশনা দেওয়া হয়েছে।

---

## ⚠️ গুরুত্বপূর্ণ তথ্য

cPanel সাধারণত PHP hosting এর জন্য তৈরি। Next.js (Node.js) deploy করতে আপনার hosting এ **Node.js সাপোর্ট** থাকতে হবে।

### ✅ cPanel এ Node.js চেক করুন
1. cPanel এ Login করুন
2. "Software" সেকশনে **"Setup Node.js App"** আছে কিনা দেখুন
3. যদি থাকে, আপনি deploy করতে পারবেন ✅
4. যদি না থাকে, hosting provider এ contact করুন

---

## 📋 Step-by-Step Deployment

### Step 1: লোকাল এ Build করুন

আপনার কম্পিউটারে এই কমান্ডগুলো রান করুন:

```bash
# Mollik-Deploy ফোল্ডারে যান
cd "e:\Saiful\Programming\Mollik-Deploy"

# Dependencies ইনস্টল করুন
npm install

# Production build তৈরি করুন
npm run build
```

Build শেষ হলে `.next` ফোল্ডার তৈরি হবে।

---

### Step 2: ZIP ফাইল তৈরি করুন

নিচের ফোল্ডার/ফাইলগুলো ZIP করুন:

```
✅ যা ZIP করতে হবে:
├── .next/           (build output)
├── public/          (static files)
├── package.json
├── package-lock.json
├── next.config.mjs
└── node_modules/    (optional - cPanel এ install করা ভালো)

❌ যা ZIP করবেন না:
├── app/             (source code - build এ আছে)
├── components/      (source code - build এ আছে)
├── data/            (source code - build এ আছে)
└── .git/
```

**সহজ উপায়:** পুরো ফোল্ডার ZIP করুন, তারপর cPanel এ unnecessary ফাইল delete করুন।

---

### Step 3: cPanel এ Upload করুন

1. **cPanel Login করুন**
   - আপনার hosting এর cPanel URL এ যান
   - Username ও Password দিন

2. **File Manager খুলুন**
   - cPanel Dashboard → File Manager

3. **public_html এ যান**
   - অথবা আপনার subdomain ফোল্ডার

4. **ZIP আপলোড করুন**
   - Upload বাটনে ক্লিক করুন
   - ZIP ফাইল সিলেক্ট করুন
   - আপলোড complete হওয়া পর্যন্ত অপেক্ষা করুন

5. **Extract করুন**
   - ZIP ফাইলে Right Click → Extract
   - Extract করুন

---

### Step 4: Node.js App Setup করুন

1. **cPanel Dashboard এ যান**

2. **"Setup Node.js App" খুলুন**
   - Software সেকশনে পাবেন

3. **"Create Application" ক্লিক করুন**

4. **নিচের তথ্য দিন:**

   | Field | Value |
   |-------|-------|
   | Node.js version | 18.x বা latest |
   | Application mode | Production |
   | Application root | `public_html` (বা আপনার ফোল্ডার) |
   | Application URL | আপনার domain |
   | Application startup file | `node_modules/next/dist/bin/next` |

5. **Environment Variables যোগ করুন:**
   ```
   Name: NODE_ENV
   Value: production
   
   Name: PORT  
   Value: (cPanel যা দেয়)
   ```

6. **"Create" বাটনে ক্লিক করুন**

---

### Step 5: Dependencies Install করুন

1. **Node.js App পেজে** আপনার app দেখতে পাবেন

2. **"Run NPM Install"** বাটনে ক্লিক করুন
   - অথবা Terminal এ: `npm install --production`

3. Install complete হওয়া পর্যন্ত অপেক্ষা করুন (2-5 মিনিট)

---

### Step 6: Application Start করুন

1. **"Start App"** বাটনে ক্লিক করুন

2. Status "Running" দেখালে সফল! ✅

3. আপনার domain visit করুন: `https://yourdomain.com`

---

## 🔧 বিকল্প পদ্ধতি: Static Export

যদি আপনার hosting এ Node.js সাপোর্ট না থাকে, Static HTML হিসেবে export করতে পারেন:

### Step 1: next.config.mjs আপডেট করুন

```javascript
/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',  // এই লাইন যোগ করুন
    images: {
        unoptimized: true,  // এই লাইন যোগ করুন
        // ... বাকি config
    },
};

export default nextConfig;
```

### Step 2: Build করুন

```bash
npm run build
```

এটি `out/` ফোল্ডারে static HTML তৈরি করবে।

### Step 3: Upload করুন

`out/` ফোল্ডারের সব কন্টেন্ট `public_html` এ আপলোড করুন।

### ⚠️ সীমাবদ্ধতা:
- API routes কাজ করবে না
- Visitor counter কাজ করবে না
- Dynamic features কাজ করবে না

---

## 🛠️ সমস্যা সমাধান

### ❌ "Application Error" দেখাচ্ছে

**সমাধান:**
1. cPanel → Setup Node.js App
2. আপনার app এ ক্লিক করুন
3. "Restart" বাটনে ক্লিক করুন
4. Error logs চেক করুন

### ❌ "502 Bad Gateway"

**সমাধান:**
1. Node.js version চেক করুন (18+ হতে হবে)
2. `npm install` আবার রান করুন
3. App restart করুন

### ❌ CSS/Images লোড হচ্ছে না

**সমাধান:**
1. `.htaccess` ফাইলে যোগ করুন:
```apache
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteRule ^(.*)$ http://127.0.0.1:PORT/$1 [P,L]
</IfModule>
```
(PORT = cPanel এ দেওয়া port)

### ❌ Build ফেইল হচ্ছে

**সমাধান:**
1. লোকাল এ build করুন
2. `.next` ফোল্ডার ZIP করে upload করুন
3. cPanel এ শুধু `npm install` করুন

---

## 📞 সাহায্য

- 📧 Email: contact@motiurrahmanmollik.com
- 🌐 Website: https://motiurrahmanmollik.com

---

## 💡 প্রস্তাবনা

cPanel এ Next.js deploy করা জটিল হতে পারে। সহজ বিকল্প:

| Platform | মূল্য | সুবিধা |
|----------|------|--------|
| **Vercel** | Free | Next.js এর জন্য সেরা, one-click deploy |
| **Netlify** | Free | সহজ, fast |
| **Railway** | Free tier | Node.js সাপোর্ট |
| **Render** | Free tier | সহজ setup |

Vercel এ deploy করতে শুধু GitHub এ push করুন, বাকি সব automatic! 🚀

---

© ২০২৬ সাইমুম শিল্পীগোষ্ঠী। সর্বস্বত্ব সংরক্ষিত।
